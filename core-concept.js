// variable:let, const

const person = {
    name: 'kuddus',
    age: 12,
    city: 'Dhaka'
}
const statement = `this person ${name} with age ${person.age}`


//condition

if (money>100) {
    
} 
else {
    
}

//array

const numbers = [34, 45, 40];
const respected = ['Motiur', 'Jahangir'];
const products = [{name: 'laptop', price: 500}, {name:'mobile', price:300}]

//loop:for and while

for (let i = 0; i < array.length; i++) {
    const element = array[i];
    
}

//function: regular and arrow
const fiveTimes =num=>num*5;
const add =(num1,num2) =>{
    const total = num1+num2
    return total;
}